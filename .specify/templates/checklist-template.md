# [CHECKLIST TYPE] Checklist: [FEATURE NAME]

**Purpose**: [Brief description of what this checklist covers]
**Created**: [DATE]
**Feature**: [Link to spec.md or relevant documentation]

**Note**: This checklist is generated by the `/speckit.checklist` command based on feature context and requirements.

<!--
  ============================================================================
  IMPORTANT: The checklist items below are SAMPLE ITEMS for illustration only.

  The /speckit.checklist command MUST replace these with actual items based on:
  - User's specific checklist request
  - Feature requirements from spec.md
  - Technical context from plan.md
  - Implementation details from tasks.md

  DO NOT keep these sample items in the generated checklist file.
  ============================================================================
-->

## Java 到 Go 迁移专项检查 🔄

> **适用场景**: 从 Java 迁移现有功能到 Go
> **目的**: 确保迁移后的功能与 Java 版本完全一致

### API 定义检查

- [ ] CHK_JV001 API 路由与 Java 版本完全匹配（包括 base path，如 `/v1/catalog` 而非 `/c`）
- [ ] CHK_JV002 请求参数名称与 Java 版本一致（如 `catalog_name` 而非 `name`）
- [ ] CHK_JV003 响应类型字段与 Java VO 一致
- [ ] CHK_JV004 路径参数（如 `:id`）已在 Request 类型中添加对应字段
- [ ] CHK_JV005 查询参数使用 `form` 标签，非 `json` 标签
- [ ] CHK_JV006 未实现 Java 中 `@Deprecated` 的接口（如有需要，明确说明）

### 业务逻辑检查

- [ ] CHK_JV010 **创建操作**: 允许在根目录（level=1）下创建子目录
- [ ] CHK_JV011 **更新操作**: 不允许修改根目录（level <= 1）
- [ ] CHK_JV012 **更新操作**: 添加循环父目录校验（新的父目录不能是自身或子孙）
- [ ] CHK_JV013 **更新操作**: 新父目录类型必须与当前目录一致
- [ ] CHK_JV014 **删除操作**: 级联删除目录及所有子目录（非拒绝删除）
- [ ] CHK_JV015 **删除操作**: 根目录（level <= 1）不允许删除
- [ ] CHK_JV016 **查询操作**: 搜索条件与 Java SQL 完全一致（包括模糊搜索条件）
- [ ] CHK_JV017 **树形查询**: 返回格式与 Java 一致（单个对象 vs 列表）
- [ ] CHK_JV025 **错误消息**: 所有错误提示必须使用中文，与 Java 版本保持一致

### 项目结构检查

- [ ] CHK_JV026 **goctl 文件命名**: goctl 使用 `--style=go_zero` 生成下划线命名文件（如 `service_context.go`、`create_catalog_logic.go`），而非驼峰命名
- [ ] CHK_JV027 **goctl 文件覆盖**: 修改 `.api` 文件后必须运行 goctl 重新生成，让 goctl 覆盖已存在文件，不要手动保留旧版本
- [ ] CHK_JV028 **Handler 一致性**: Handler 中的请求类型必须与 Logic 函数签名一致
- [ ] CHK_JV029 **Model 子包导出**: model 子包（如 `catalog/catalog`）需创建父包（`catalog/catalog.go`）导出类型
- [ ] CHK_JV030 **ServiceContext 初始化**: `main.go` 中需正确处理 `NewServiceContext` 的错误返回值

### 数据访问层检查

- [ ] CHK_JV020 Model 方法签名与 Java Mapper 方法对应
- [ ] CHK_JV021 软删除使用 `deleted_at` 字段，而非硬删除
- [ ] CHK_JV022 UUID v7 用于主键，而非 Snowflake ID
- [ ] CHK_JV023 批量操作方法已实现（如 `DeleteBatch`）
- [ ] CHK_JV024 辅助方法已实现（如 `GetIDList` 用于获取子孙节点）

### 常见错误模式检查 ⚠️

| 错误模式 | 说明 | 检查项 |
|---------|------|--------|
| **Base Path 不一致** | Go 用 `/c`，Java 用 `/v1/catalog` | CHK_JV001 |
| **路径参数缺失** | `put /:id` 但 Request 无 `Id` 字段 | CHK_JV004 |
| **参数名不匹配** | Java 用 `catalog_name`，Go 用 `name` | CHK_JV002 |
| **条件判断错误** | `if name != ""` 应在条件内应用 LIKE | CHK_JV016 |
| **逻辑反转** | 拒绝在根目录下创建子目录 | CHK_JV010 |
| **删除逻辑错误** | 有子目录就拒绝删除，应级联删除 | CHK_JV014 |
| **循环引用缺失** | 更新时未检查循环父目录 | CHK_JV012 |
| **类型不匹配** | 返回自定义类型而非 Java VO | CHK_JV003 |
| **语言不一致** | Java 中文错误消息，Go 变成英文 | CHK_JV025 |
| **goctl 命名误解** | goctl 生成下划线命名文件，非驼峰命名 | 必须理解 goctl 使用 `--style=go_zero` 生成下划线命名文件 |
| **goctl 文件覆盖** | 修改 .api 后旧文件仍保留 | 修改 .api 后必须重新生成，让 goctl 覆盖所有文件 | 修改 .api 后运行 goctl 重新生成，不要手动保留旧文件 | CHK_JV027 |
| **Handler 请求类型** | Handler 使用的请求类型与 Logic 不一致 | CHK_JV028 |
| **子包导出** | model 子包需要创建中间包文件导出类型 | CHK_JV028 |
| **ServiceContext 返回值** | `NewServiceContext` 返回 `(*ServiceContext, error)` | CHK_JV029 |

---

## 代码质量检查

- [ ] CHK001 代码格式化 (`gofmt -w .`)
- [ ] CHK002 代码检查通过 (`golangci-lint run`)
- [ ] CHK003 无编译错误 (`go build ./...`)
- [ ] CHK004 无未使用的导入或变量

## 测试检查 🧪

- [ ] CHK010 所有 `*_test.go` 文件已创建
- [ ] CHK011 单元测试全部通过 (`go test ./...`)
- [ ] CHK012 测试覆盖率 > 80% (`go test ./... -cover`)
- [ ] CHK013 关键路径 100% 测试覆盖
- [ ] CHK014 边界条件测试已添加
- [ ] CHK015 错误处理测试已添加
- [ ] CHK016 测试命名符合规范: `Test{Function}_{Scenario}_{Result}`

## API 检查

- [ ] CHK020 API 定义完整 (`*.api` 文件)
- [ ] CHK021 Request/Response 类型正确定义
- [ ] CHK022 Swagger 文档已生成 (`make swagger`)
- [ ] CHK023 API 路径符合 RESTful 规范

## 数据库检查

- [ ] CHK030 DDL 文件已创建 (`migrations/`)
- [ ] CHK031 Model 接口已定义 (`interface.go`)
- [ ] CHK032 CRUD 方法已实现
- [ ] CHK033 数据库索引已添加

## 文档检查

- [ ] CHK040 代码注释完整
- [ ] CHK041 README 已更新 (如有需要)
- [ ] CHK042 CHANGELOG 已更新

## Notes

- Check items off as completed: `[x]`
- Add comments or findings inline
- Link to relevant resources or documentation
- Items are numbered sequentially for easy reference
- **所有测试检查项必须完成才能合并代码**
